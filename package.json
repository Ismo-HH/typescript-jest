{
  "scripts": {
    "test": "vitest run --coverage --reporter=default --reporter=html"
  },
  "devDependencies": {
    "vitest": "^4.0.9",
    "@vitest/coverage-v8": "^4.0.10",
    "@vitest/ui": "^4.0.10"
  },
  "type": "module"
}
